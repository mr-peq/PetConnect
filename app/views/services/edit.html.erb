<%# to do : partial with the new_form %>

<%= simple_form_for(@service) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :availabilities %>
  <%= f.input :address %>
  <%= f.submit %>
<% end %>
